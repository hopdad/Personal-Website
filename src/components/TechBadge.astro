---
export interface Props {
  label: string;
}
const { label } = Astro.props;
---

<span
  class="inline-flex items-center rounded-btn border px-2 py-0.5 text-caption"
  style="border-color: var(--border); color: var(--text-muted);"
>
  {label}
</span>
